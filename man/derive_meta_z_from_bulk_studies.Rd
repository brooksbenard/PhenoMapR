% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_reference.R
\name{derive_meta_z_from_bulk_studies}
\alias{derive_meta_z_from_bulk_studies}
\title{Derive a pan-cohort meta-z reference from multiple bulk studies}
\usage{
derive_meta_z_from_bulk_studies(
  studies,
  meta_z_label = "meta_z",
  hugo_species = c("human", "mouse"),
  binary_positive_reference = c("second", "first"),
  verbose = TRUE
)
}
\arguments{
\item{studies}{A named list. Each element must be a list with at least
\code{bulk_expression} and \code{phenotype}. Optional per-study fields are
forwarded to \code{derive_reference_from_bulk()} (e.g. \code{phenotype_type},
\code{platform}, \code{probe_annotation}).}

\item{meta_z_label}{Character label for the output z-score column
(default \code{"meta_z"}).}

\item{hugo_species}{Species for HGNC symbol validation (\code{"human"} or
\code{"mouse"}); passed to each per-study call.}

\item{binary_positive_reference}{For binary phenotypes, which level is the
positive reference (\code{"second"} or \code{"first"}); passed to each
per-study call.}

\item{verbose}{Logical.}
}
\value{
A data.frame of combined meta-z scores suitable for \code{\link{PhenoMap}}.
}
\description{
Computes per-study gene z-scores with \code{\link{derive_reference_from_bulk}}
and combines them with a Stouffer weighted meta-analysis (weights =
\code{sqrt(n_samples)} per study), analogous to PRECOG / TCGA meta-z signatures.
}

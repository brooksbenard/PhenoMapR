% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{is_likely_normalized}
\alias{is_likely_normalized}
\title{Check if expression matrix is likely already normalized (e.g. TISCH2, log-norm)}
\usage{
is_likely_normalized(x, sample_cap = 5000L)
}
\arguments{
\item{x}{Matrix or Matrix (genes x cells).}

\item{sample_cap}{Max number of values to sample (default 5000).}
}
\value{
TRUE if data is likely already normalized; FALSE if likely raw counts.
}
\description{
Used to avoid double-normalization when data is pre-normalized. Heuristic:
samples values and returns TRUE if most are non-integer or range suggests
log-scale (e.g. max moderate and not raw counts).
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visium_spatial_sync.R
\name{sync_visium_spatial_image_from_10x_dir}
\alias{sync_visium_spatial_image_from_10x_dir}
\title{Replace Visium slide images using 10X \code{spatial} outputs}
\usage{
sync_visium_spatial_image_from_10x_dir(
  object,
  spatial_dir,
  filter.matrix = FALSE,
  image.name = "tissue_lowres_image.png"
)
}
\arguments{
\item{object}{A \link[Seurat:Seurat-class]{Seurat::Seurat} object with at least one Visium image.}

\item{spatial_dir}{Path to the 10X \strong{\code{spatial}} directory.}

\item{filter.matrix}{Passed to \code{\link[Seurat:Read10X_Image]{Seurat::Read10X_Image()}}. Use \code{FALSE} (default)
to load all tissue positions, then keep only \code{\link[=colnames]{colnames()}} of \code{object}.}

\item{image.name}{PNG file name in \code{spatial_dir} (default lowres, as in Seurat).}
}
\value{
\code{object} with image slot(s) replaced for cells that overlap the new image.
}
\description{
Rebuilds each image slot with \code{\link[Seurat:Read10X_Image]{Seurat::Read10X_Image()}}, which reads
\code{tissue_positions_list.csv} or \code{tissue_positions.csv}, \code{scalefactors_json.json},
and the tissue PNG. This matches \strong{spot centroids to the H&E} as Space Ranger
defines them—useful when a saved \code{.rds} has drifted or was built with
inconsistent coordinate logic.
}
\details{
Set \code{spatial_dir} to the \strong{\code{spatial} folder} inside the Visium run (the directory
that contains the CSV, JSON, and \code{tissue_lowres_image.png}).

\strong{Barcodes:} only spots/cells whose names appear in both \code{object} and the 10X
positions file are kept. If your object uses renamed barcodes (e.g. some
deconvolution outputs), intersection may be empty and the image slot is left
unchanged (a warning is issued).
}
\examples{
\dontrun{
## After Load10X_Spatial / download, if spots look misaligned:
obj <- sync_visium_spatial_image_from_10x_dir(obj, spatial_dir = "/path/to/spatial")
}
}

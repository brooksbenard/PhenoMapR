# Minimum cells per phenotype tail required for `within_cell_type` cell-type-specific marker contrasts.

Minimum cells per phenotype tail required for `within_cell_type`
cell-type-specific marker contrasts.

## Usage

``` r
.MIN_CELLS_PER_PHENO_TAIL_FOR_CT_MARKERS
```

## Format

An object of class `integer` of length 1.

# Default phenotype-group colors for marker heatmaps

Default phenotype-group colors for marker heatmaps

## Usage

``` r
.default_phenotype_palette()
```

## Value

Named character vector (`Most Favorable`, `Other`, `Most Adverse`).

# Build marker heatmap color scheme list with defaults filled in

Build marker heatmap color scheme list with defaults filled in

## Usage

``` r
.normalize_marker_heatmap_color_schemes(color_schemes)
```

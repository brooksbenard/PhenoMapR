# Shuffle reference z-scores among signature genes (permutation null).

Shuffle reference z-scores among signature genes (permutation null).

## Usage

``` r
permute_reference_z(meta_z, seed = NULL)
```
